import WorkContent from "./WorkContent";

export const metadata = {
  title: "Sample modernization — Revanio",
  description: "A representative legacy warehouse system, modernized end-to-end with zero downtime. See exactly how Revanio works.",
};

export default function Work() {
  return <WorkContent />;
}
